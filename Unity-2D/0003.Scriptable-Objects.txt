using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(menuName = "State")]
public class State : ScriptableObject
{
    [TextArea(10, 14)]	// 'Inspector' tab setting (Unity)
    // 10 is the number of lines in box (adding more will cause scroll effect).
    // 14 is the inital number of lines in the box height
    [SerializeField]
    string storyText;
}
