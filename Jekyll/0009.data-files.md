Something like mini-database.

Steps:
* Create `_data` directory.
* Supported formats: `.json`, `.yml`, `.csv`.
* Create a `people.json` file (any name will work though)

```html

{{ site.data.people.someProperty }}

```
