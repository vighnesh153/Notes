using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class AdventureGame : MonoBehaviour
{
    [SerializeField]	// This decorator is needed so that this field can be edited in the unity window
    Text textComponent;

    // Start is called before the first frame update
    void Start()
    {
        textComponent.text = "Hello. This is a game information message! " +
            "Please read me. Or I will kill you.";
    }

    // Update is called once per frame
    void Update()
    {

    }
}
